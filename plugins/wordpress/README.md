# WordPress 插件

此目錄預留給 MVP 的 WordPress 插件。

第一版插件目標：

- 提供站點連接設定頁。
- 與 SaaS API 完成 Token 綁定。
- 透過 WordPress REST API 或自定義命名空間同步 Posts、Pages、Media。
- 支援修改前預覽、應用、快照和回滾。

限制：

- 不保存 SaaS 主帳號密碼。
- 不直接無審核批量發布內容。
- 不提交任何 `.env` 或真實 API Key。
